<ul><li> Select the first column, and then click on the arrow under <code style="inline">PivotTable</code> in the <code style="inline">Tables</code> group in the <code style="inline">Insert</code> tab, and choose <code style="inline">Pivot Table</code> from the drop-down menu.</li><li> Click <code style="inline">OK</code>.</li><li> Excel will create a new worksheet with a blank table.  We have to tell Excel how to construct the table:</li><li>You should see a window titled <code style="inline">PivotTable Field List</code> with our single variable, <code style="inline">Party 1</code>. </li><li>Check the box next to <code style="inline">Party 1</code>.</li><li>Now drag the <code style="inline">Party 1</code> item to the section in the PivotTable Field List labeled <code style="inline">Values</code>.</li><li>Your table should now have two columns.  The first column of the table lists the 30 birthdays from Party 1 in order from smallest to largest. The second column with the heading <code style="inline">Count of Party 1</code> contains the number of times that the birthday in the first column appeared in the sample of size 30. If more than 1 person at this party has the same birthday, we call this a match.</li><li>Repeat this process for Party 2 through Party 10.</li></ul>