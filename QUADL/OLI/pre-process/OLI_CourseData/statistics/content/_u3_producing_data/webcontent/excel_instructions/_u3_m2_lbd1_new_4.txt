<ul><li><code style="inline">min</code></li><li>In the cell to the right of <code style="inline">min</code> and underneath <code style="inline">Treatment 1</code>, type <code style="inline">=min([range])</code>, where [range] represents the data in the <code style="inline">Age_SRS</code> column that has a Treatment value of 1.</li><li>Now move one cell to the right (so that you're underneath <code style="inline">Treatment 2</code> ), and type <code style="inline">=min([range])</code> where [range] represents the data in the <code style="inline">Age_SRS</code> column that has a Treatment value of 2.</li><li>Repeat this calculation for Treatment 3, again adjusting the data range appropriately.</li><li><code style="inline">Q1</code></li><li>In the cell to the right of <code style="inline">Q1</code> and underneath <code style="inline">Treatment 1</code>, type <code style="inline">=quartile([range],1)</code>, where [range] represents the data in the <code style="inline">Age_SRS</code> column that has a Treatment value of 1.  Repeat for Treatment 2 and 3, adjusting the data range appropriately each time.</li><li><code style="inline">Median</code></li><li>In the cell to the right of <code style="inline">Median</code> and underneath <code style="inline">Treatment 1</code>, type <code style="inline">=median([range])</code>, where [range] represents the data in the <code style="inline">Age_SRS</code> column that has a Treatment value of 1.  Repeat for Treatment 2 and 3, adjusting the data range appropriately each time.</li><li><code style="inline">Q3</code></li><li>In the cell to the right of <code style="inline">Q3</code> and underneath <code style="inline">Treatment 1</code>, type <code style="inline">=quartile([range],3)</code>, where [range] represents the data in the <code style="inline">Age_SRS</code> column that has a Treatment value of 1.  Repeat for Treatment 2 and 3, adjusting the data range appropriately each time.</li><li><code style="inline">Max</code></li><li>In the cell to the right of <code style="inline">Max</code> and underneath <code style="inline">Treatment 1</code>, type <code style="inline">=max([range])</code>, where [range] represents the data in the <code style="inline">Age_SRS</code> column that has a Treatment value of 1.  Repeat for Treatment 2 and 3, adjusting the data range appropriately each time.</li></ul>