<ul><li>Pick an empty column in your Excel worksheet and label it <code style="inline">Treatment</code>.</li><li>In the <code style="inline">Treatment</code> column, type this formula in row 2: <code style="inline">=RANDBETWEEN(1,3)</code>.  Then copy and paste it into each cell in the column from row 3 down to row 451.  This generates a random integer from 1 to 3 in each cell of the column.</li><li>Select the entire column of random numbers that you just created, and choose <code style="inline">Edit -&gt; Copy</code> and then <code style="inline">Edit -&gt; Paste Special</code> from the menubar.</li><li>In the pop-up window, choose <code style="inline">Values</code> under <code style="inline">Paste</code>, and click <code style="inline">OK</code>.</li><li>Type Control-A to select all of the data in the worksheet, and then choose <code style="inline">Data -&gt; Sort</code> from the menubar.</li><li>In the pop-up window, make sure that <code style="inline">Header row</code> is selected and then choose <code style="inline">Treatment</code> under <code style="inline">Sort by</code>.  Click <code style="inline">OK</code>.</li></ul>