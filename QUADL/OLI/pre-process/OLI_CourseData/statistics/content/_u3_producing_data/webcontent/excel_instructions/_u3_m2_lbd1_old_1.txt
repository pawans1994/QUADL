<ul><li>Pick four empty columns in your Excel worksheet and label them <code style="inline">RandomNumber</code>, <code style="inline">Age_SRS</code>, <code style="inline">Gender_SRS</code>, and <code style="inline">Comp_SRS</code>.</li><li>In the <code style="inline">RandomNumber</code> column, type this formula in row 2: <code style="inline">=RAND()</code>.  Then copy and paste the formula into each cell in the column from row 3 down to row 20784.  This generates a random number in each cell of the column.</li><li>Select the entire column of random numbers that you just created, and choose <code style="inline">Edit -&gt; Copy</code> and then <code style="inline">Edit -&gt; Paste Special</code>  from the menubar.</li><li>In the pop-up window, choose <code style="inline">Values</code> under <code style="inline">Paste</code>, and click <code style="inline">OK</code>.</li><li>Type Control-A to select all of the data in the worksheet, and then choose <code style="inline">Data -&gt; Sort</code> from the menubar.</li><li>In the pop-up window, make sure that <code style="inline">Header row</code> is selected and then choose <code style="inline">RandomNumber</code> under <code style="inline">Sort by</code>.  Click <code style="inline">OK</code>.</li><li>Now select all of the data in rows 2-451 under the columns that contain the original variables (Age, Gender, Comp), and copy and paste those data to the new columns you created (Age_SRS, Gender_SRS, and Comp_SRS).</li></ul>