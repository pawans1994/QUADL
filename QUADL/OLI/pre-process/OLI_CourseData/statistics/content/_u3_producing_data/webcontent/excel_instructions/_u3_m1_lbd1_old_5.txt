<ul><li>Type Control-A to select all of the data in the worksheet, and then choose <code style="inline">Data -&gt; Sort</code> from the menubar.</li><li>In the pop-up window, make sure that <code style="inline">Header row</code> is selected and then choose <code style="inline">Course</code> under <code style="inline">Sort by</code>.  Click <code style="inline">OK</code>.</li><li>Now pick four empty columns and label them <code style="inline">Handed_Business</code>, <code style="inline">Sex_Business</code>, <code style="inline">Verbal_Business</code>, and <code style="inline">Age_Business</code>.</li><li>Finally, copy the data for our four original variables (</code>Handed</code>, <code style="inline">Sex</code>, <code style="inline">Verbal</code>, and <code style="inline">Age</code>) from the rows where the course is <code style="inline">Business</code> and paste them in the new columns.</li></ul>