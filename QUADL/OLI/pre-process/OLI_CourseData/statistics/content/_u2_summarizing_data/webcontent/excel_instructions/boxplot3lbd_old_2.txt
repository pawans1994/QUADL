<ul><li><code style="inline">Q1</code></li><li>In the cell to the right of <code style="inline">Q1</code> and underneath <code style="inline">College A</code>, type <code style="inline">=quartile(B2:B9,1)</code> because B2 through B9 represents the data for College A.</li><li>Now move one cell to the right (so that you're underneath <code style="inline">College B</code>, and type <code style="inline">=quartile(B10:B17,1)</code> because B10 through B17 represents the data for College B.</li><li>Repeat this calculation for colleges C, D, E, and F, adjusting the data range appropriately for each college.</li><li><code style="inline">min</code></li><li>In the cell to the right of <code style="inline">min</code> and underneath <code style="inline">College A</code>, type <code style="inline">=min(B2:B9)</code>, and repeat for colleges B, C, D, E, and F.</li><li><code style="inline">Median</code></li><li>In the cell to the right of <code style="inline">Median</code> and underneath <code style="inline">College A</code>, type <code style="inline">=median(B2:B9)</code>, and repeat for colleges B, C, D, E, and F.</li><li><code style="inline">Max</code></li><li>In the cell to the right of <code style="inline">Max</code> and underneath <code style="inline">College A</code>, type <code style="inline">=Max(B2:B9)</code>, and repeat for colleges B, C, D, E, and F.</li><li><code style="inline">Q3</code></li><li>In the cell to the right of <code style="inline">Q3</code> and underneath <code style="inline">College A</code>, type <code style="inline">=quartile(B2:B9,3)</code>, and repeat for colleges B, C, D, E, and F. </li></ul>