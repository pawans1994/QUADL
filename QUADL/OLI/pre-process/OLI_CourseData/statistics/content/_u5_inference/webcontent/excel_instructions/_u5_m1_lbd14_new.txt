Once you have calculated the z-statistic, you can calculate the p-value using the Excel function <code style="inline">NORMSDIST</code>.  In this case, the p-value we want is <code style="inline">NORMSDIST(z-statistic)</code>.