<ul><li>First, we need to calculate x-bar, the sample mean.  To do this, we use the <code style="inline">AVERAGE</code> function in Excel - in this case, <code style="inline">=AVERAGE(A2:A76)</code>.</li><li>Next, we calculate s, the sample standard deviation using the <code style="inline">STDEV</code> function in Excel over the same range of data.</li><li>We know &#956;o and n from reading the problem statement, so we can now calculate a value for t.</li><li>Finally, we find our p-value using the Excel function <code style="inline">TDIST(t, n-1, 2)</code>.</li></ul>