<ul><li>To create this table, first make the row and column labels: in an empty section of your worksheet, pick a cell and type <code style="inline">Statistic</code>.  Then move one cell to the right and type <code style="inline">Blue</code>.  Move one cell to the right and type <code style="inline">Brown</code>, and then another cell to the right and type <code style="inline">Green</code>.  Now, in the cell  underneath <code style="inline">Statistic</code>, type <code style="inline">min</code>.  Move down one cell and type <code style="inline">Q1</code>, then underneath that <code style="inline">Median</code>, then <code style="inline">Q3</code>, then <code style="inline">Max</code>, then <code style="inline">St Dev</code>, and finally, <code style="inline">n</code>.</li></ul>