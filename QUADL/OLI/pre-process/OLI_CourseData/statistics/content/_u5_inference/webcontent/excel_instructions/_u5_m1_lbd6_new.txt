<ul><li>First, we need to calculate X (the number of people surveyed who are satisfied with the expiration of the law). These people are represented by a '1' in the dataset. To count all of the '1's, we use the formula <code style="inline">=COUNTIF([range],1)</code>, where [range] is the range of data we want to count over - in this case, A1:A1200.</li><li>Since we're looking for a 95% confidence interval, we'll use a value of 2 for z*.</li></ul>