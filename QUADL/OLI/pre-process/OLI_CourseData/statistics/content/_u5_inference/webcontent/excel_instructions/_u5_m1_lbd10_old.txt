Once we have calculated our test statistic z, we find the probability that p is less than the test statistic z using the Excel formula <code style="inline">=NORMSDIST(z)</code>, which in this case, is the p-value that we are looking for (i.e. there is no need to subtract from 1 this time).